<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ntsed.api.dao.UserInfoMapper">
<!-- author: セツ
  date:2023/12/19  -->

   <select id="findAll" resultType="com.ntsed.api.entity.UserInfo">
        SELECT * FROM employee2 WHERE DEL_FLG = 0
    </select>
    <select id="findById" resultType="com.ntsed.api.entity.UserInfo">
        SELECT * FROM employee2 WHERE EMPLOYEE_ID=#{EMPLOYEE_ID} AND WHERE DEL_FLG = 0
    </select>

	<insert id="save">
		INSERT INTO employee2
		(EMPLOYEE_ID,
		EMPLOYEE_NAME,
		DEPT_ID,
		DEL_FLG,
		GENDER_ID,
		EMPLOYEE_TEL,
		ENTRY_DATE,
		EMPLOYEE_AGE,
		EMPLOYEE_MAIL,
		CREATE_DATE,
		CREATE_USER)
		VALUES
		(#{EMPLOYEE_ID},
		#{EMPLOYEE_NAME},
		#{DEPT_ID},
		#{DEL_FLG},
		#{GENDER_ID},
		#{EMPLOYEE_TEL},
		#{ENTRY_DATE},
		#{EMPLOYEE_AGE},
		#{EMPLOYEE_MAIL},
		CURRENT_TIMESTAMP,
		#{LOGIN_USER}

		)
	</insert>
	<update id="update">
		UPDATE employee2 SET EMPLOYEE_NAME = #{EMPLOYEE_NAME}, DEPT_ID = #{DEPT_ID},
		GENDER_ID = #{GENDER_ID},
		EMPLOYEE_TEL =#{EMPLOYEE_TEL},
		ENTRY_DATE
		=#{ENTRY_DATE},
		EMPLOYEE_AGE =#{EMPLOYEE_AGE},
		EMPLOYEE_MAIL
		=#{EMPLOYEE_MAIL},
		UPDATE_USER= #{EMPLOYEE_NAME}, UPDATE_DATE = CURRENT_TIMESTAMP WHERE EMPLOYEE_ID =
		#{EMPLOYEE_ID}
	</update>


</mapper>